import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    navContainer: {
        alignItems: 'center',
    },
    img: {
        width: 120,
        height: 50,
    },

})